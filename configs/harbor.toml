# harbor — Terminal-Bench 2, each task run in its own declared container image (needs docker).
# The `bash` harness gives the agent the bash (+ edit) tools it drives the terminal with.
#
#   uv run eval @ configs/harbor.toml
num_tasks = 10

[env.taskset]
id = "harbor"
dataset = "terminal-bench/terminal-bench-2"

[env.agent]
runtime = { type = "docker" }

[env.agent.harness]
id = "bash"
